import React from 'react';
import SearchBar from "../../shared/SearchBar.tsx";

const JobSearch = () => {

    return (
        <SearchBar placeholder={"Job title, company or address"} />
    );
}

export default JobSearch;
