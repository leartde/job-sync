import React from 'react';
import SearchBar from "../../shared/SearchBar.tsx";

const Search = () => {
    return (
        <SearchBar placeholder={"Employer name"}   />
    );
};

export default Search;

